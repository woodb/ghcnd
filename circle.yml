machine:
    python:
        version: 2.7.10

dependencies:
    pre:
        - ln -s ~/.pyenv/versions/2.6.8/bin/python2.6 ~/bin
        - ln -s ~/.pyenv/versions/2.7.10/bin/python2.7 ~/bin
        - ln -s ~/.pyenv/versions/3.3.3/bin/python3.3 ~/bin
        - ln -s ~/.pyenv/versions/3.4.3/bin/python3.4 ~/bin
    override:
        - pip install -U pip
        - pip install -U tox

test:
    override:
        - env PATH="$HOME/bin:$PATH" tox -eflake8:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy26-pandas_11:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy26-pandas_12:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy26-pandas_13:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy26-pandas_14:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy26-pandas_15:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy26-pandas_16:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy26-pandas_latest:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy27-pandas_11:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy27-pandas_12:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy27-pandas_13:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy27-pandas_14:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy27-pandas_15:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy27-pandas_16:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy27-pandas_latest:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy33-pandas_11:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy33-pandas_12:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy33-pandas_13:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy33-pandas_14:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy33-pandas_15:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy33-pandas_16:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy33-pandas_latest:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy34-pandas_11:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy34-pandas_12:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy34-pandas_13:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy34-pandas_14:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy34-pandas_15:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy34-pandas_16:
            parallel: true
        - env PATH="$HOME/bin:$PATH" tox -epy34-pandas_latest:
            parallel: true
